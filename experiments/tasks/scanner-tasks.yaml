---
  apiVersion: tekton.dev/v1alpha1
  kind: Task
  metadata:
    name: scanner-task
  spec:
    inputs:
      resources:
        - name: git-source
          type: git
    steps:
    - name: scan-it
      image: # todo
      command:
      - # todo 
      args:
      - # todo